import 'package:flutter/material.dart';

class Headeah extends StatelessWidget {
  const Headeah({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
